class Solution {
    public boolean isFlipedString(String s1, String s2) {
        if (s1.length() != s2.length()) {
            return false;
        }
        if (s2.equals(s1)) {
            return true;
        }
        s1 += s1;
        return s1.contains(s2);
    }
}
