 public char nextGreatestLetter(char[] letters, char target) {
        for (int i = 0; i < letters.length; i++) {
            //比 target 字符大时。
            if (letters[i] > target) {
                return letters[i];
            }
        }
        //当 target = 'z' 时
        return letters[0];
    }
